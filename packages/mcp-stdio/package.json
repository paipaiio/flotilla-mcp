{
  "name": "flotilla-mcp",
  "version": "0.1.0",
  "description": "Multi-server SSH MCP server: fan-out execution, rolling deploys with circuit breaking, cross-host diffing.",
  "type": "module",
  "bin": {
    "flotilla-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@flotilla/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.18.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
